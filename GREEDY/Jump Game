class Solution {
public:
    bool canJump(vector<int>& nums) {
        int remain=nums[0];
        int n=nums.size();

        for(int i=0; i<n; i++){
            if(remain<0)return false;
            if(nums[i]>remain)remain=nums[i];
            remain--;
        }
        return true;
    }
};